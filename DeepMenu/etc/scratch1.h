touch
    begin
        root //now,before => next,before
            (0,0),(1,0) => (1,0),(0,0) // swap
            (0,0),(0,1) => (0,1),(0,0) // swap
            (0,0),(0,0) => (1,1),(0,0) // trunks
            (0,0),(1,1) => (1,1),(0,0) // trunks

        (*,n) => (*,0),()
